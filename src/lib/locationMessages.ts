/** Shown when the user denies geolocation after an explicit in-app action. */
export const LOCATION_DENIED_USER_MESSAGE =
  "Location access denied. Enable it in your browser settings to use this feature.";

/** Extra context for iOS / Safari when the site is “Allow” but the API still fails. */
export const LOCATION_DENIED_TROUBLESHOOT_HINT =
  "If you already chose Allow: turn on Settings → Privacy & Security → Location Services, allow Safari (or your browser) to use location, and open the same URL you allowed (gellog.app and www.gellog.app are separate).";
