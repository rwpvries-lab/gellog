import type { GelatoTokens } from "@/src/lib/gelato-tokens";

/** Neutral scoop for empty states and icons (no flavour data). */
export const PLACEHOLDER_GELATO_TOKENS: GelatoTokens = {
  base: "cream",
  drizzle: "none",
  crumble: "none",
};
